# Leave unset for local dev — Vite proxies /api to localhost:8000 (vite.config.js).
# In production, set this to your deployed backend's API URL, e.g.:
VITE_API_URL=https://your-backend.onrender.com/api
